import Config from '../libs/config';
beforeAll(async () => {
    let jwtToken = "key"
    let config = (new Config()).settings;
})
describe('Test Socket paths', () => {
    it('should be ok', async () => {
        //let time = (await Request.findOne(1)).requestTimestamp;
        //console.log(time);
    })
})