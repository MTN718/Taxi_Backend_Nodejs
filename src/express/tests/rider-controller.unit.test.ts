
describe('Login', () => {

    beforeAll(async () => {
    })

    it('Correct credentials logins', async () => {
        /*typeorm.getRepository = jest.fn().mockReturnValue({
                findOne: jest.fn().mockResolvedValue(_rider)
        })*/
        //await expect(route.login('1')).resolves.toBeDefined()
    })

    it('Incorrect credentials fails', async () => {
        //await expect(route.login('1')).rejects.toThrow(InvalidCredentialsException)
    })
})