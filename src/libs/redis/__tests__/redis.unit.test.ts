import Redis from "../../redis/redis";
let redis: Redis
beforeAll(() => {
  
})
describe('Driver Location', () => {
  it('OK', async () => {
    expect(1).toBe(1)
  })
})

describe('Requests', () => {
  
})

describe('Cleaner', () => {
  
})

afterAll(async () => {
  
})