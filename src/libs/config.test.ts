import Config from "./config";
var dev: Config;
var prod: Config;
var test: Config;
describe('Test Configuration file', () => {
    beforeAll(() => {
    })
    it('Should show database name', () => {
        expect(1).toBe(1)
    })
})